@using BussinessObject;
@model List<Book>
<div class="m-5 border-2" style="border: 2px solid white;">
    @using (Html.BeginForm("Index", "Home", FormMethod.Post, new { @class = "form-inline card-body" }))
    {
        <div class="form-group">
            <label for="email">Book Title:</label>
            @Html.TextBox("BookTitle", "", new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label for="email">Book Author:</label>
            @Html.TextBox("BookAuthor", "", new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label for="email">ISBN:</label>
            @Html.TextBox("ISBN", "", new { @class = "form-control" })
        </div>
            <input type="submit" class="btn btn-info" value="search" />



            }
        </div>
        @{
            Html.RenderPartial("_GetBooks", Model);
        }
